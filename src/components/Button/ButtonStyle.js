import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#277BC0",
        borderRadius: 5,
        padding: 10,
        alignItems: "center",
        margin: 15
    },
    text: {
        color: "white",
        fontWeight: "bold"
    }
})